import React from 'react';
import SiteLayout from '../../layout/SiteLayout';

const kanban = () => {
    return (
        <SiteLayout>
            칸반보드
        </SiteLayout>
    );
};

export default kanban;