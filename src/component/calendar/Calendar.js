import React from 'react';
import SiteLayout from '../../layout/SiteLayout';

const calendar = () => {
    return (
        <SiteLayout>
        <div>
            캘린더
        </div>
        </SiteLayout>
    );
};

export default calendar;